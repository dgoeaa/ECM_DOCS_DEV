# Files accepted as already carrying a Power Automate signature.
#
# EMPTY, deliberately. ECM_DOCS_DEV.zip was the only entry; it held signed trigger URLs for
# 25 workflows and was removed from the working tree once its irreplaceable content had been
# extracted to docs/reference/.
#
# Do not add to this list to make a build pass. A new entry means a credential was committed
# and then accepted instead of removed — and the fix is to rotate the trigger in Power
# Automate, because deleting the file revokes nothing.
