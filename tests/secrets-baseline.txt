# Known-affected files carrying Power Automate SAS signatures.
#
# This list may only SHRINK. A file appears here because its signatures were committed
# before the ratchet existed. Deleting a file revokes nothing — the workflow it names keeps
# answering until that trigger is deleted or regenerated in Power Automate.
#
# Scope is 31 distinct signatures across 25 development/pilot Power Automate workflows — see
# docs/architecture/ROTATION_REGISTER.md. Those endpoints are to be REPLACED ENTIRELY at
# cutover rather than rotated in place, so this file is not a countdown to a rotation; it is
# the ratchet that stops NEW signatures entering the tree while development continues.
#
# document-portal/js/data.js was removed from this list at step 5: the portal now talks to
# the authenticating proxy and holds no credential at all. Its three workflows still have to
# be decommissioned at cutover — deleting the file revoked nothing — but there is no longer
# a file to baseline.
#
# ECM_DOCS_DEV.zip holds all 31 across 18 of its 837 members. Nine appear nowhere else and
# were invisible to every control here until the scanner learned to read archive members.
#
ECM_DOCS_DEV.zip
