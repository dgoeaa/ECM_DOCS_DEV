# Known-affected files carrying Power Automate SAS signatures.
#
# This list may only SHRINK. A file appears here because its signatures were committed
# before the ratchet existed; rotation in Power Automate must happen BEFORE the file is
# cleaned, because deleting a file revokes nothing.
#
# Rotation scope is 31 distinct signatures across 25 Power Automate workflows — see
# docs/architecture/ROTATION_REGISTER.md.
#
# document-portal/js/data.js was removed from this list at step 5: the portal now talks to
# the authenticating proxy and holds no credential at all. Its three signatures still need
# rotating — deleting them revoked nothing — but there is no longer a file to baseline.
#
# ECM_DOCS_DEV.zip holds all 31 across 18 of its 837 members. Nine appear nowhere else and
# were invisible to every control here until the scanner learned to read archive members.
#
ECM_DOCS_DEV.zip
