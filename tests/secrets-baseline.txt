# Files carrying a Power Automate SAS signature that were ALREADY exposed when the ratchet
# was introduced (gap G-03 in CAPABILITY_ASSESSMENT_R11.6.md). The scanner reports these
# and fails on anything NEW; the list may only shrink.
#
# A signature is a bearer credential. Removing a file revokes NOTHING — every signature
# listed here must be ROTATED in Power Automate, and remains valid until it is, because it
# is still readable in Git history regardless of the current tree.
#
# document-portal/js/data.js was cleared: its three signatures moved to the git-ignored
# config.local.js (see document-portal/config.example.js). THOSE THREE STILL NEED ROTATION.
#
# ECM_DOCS_DEV.zip was invisible to this check until the scanner learned to read archives.
# It carries more distinct signatures than the rest of the tree combined, plus an
# "UNREDACTED" operations manifest. It should be removed from the repository entirely —
# it is a 17 MB build artefact, not source — but only after its signatures are rotated.
ECM_DOCS_DEV.zip
newack/config.js
