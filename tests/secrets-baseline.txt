# Known-affected files carrying Power Automate SAS signatures.
#
# This list may only SHRINK. A file appears here because its signatures were committed
# before the ratchet existed; rotation in Power Automate must happen BEFORE the file is
# cleaned, because deleting a file revokes nothing.
#
# Rotation scope is 31 distinct signatures, not the 4 visible in the two source files
# below. ECM_DOCS_DEV.zip holds 31 across 18 of its 837 members: 22 also appear in git
# history, and 9 appear nowhere else at all. Until the scanner learned to read archive
# members it skipped every binary, so those 9 were invisible to every control here.
#
document-portal/js/data.js
newack/config.js
ECM_DOCS_DEV.zip
