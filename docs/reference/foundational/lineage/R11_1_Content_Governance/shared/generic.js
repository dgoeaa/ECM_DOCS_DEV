import{State}from'../core/state.js';import{head,esc}from'../core/ui.js';export const generic=(t,s)=>async el=>{const x=State.get();el.innerHTML=`<div class=workspace>${head(t,s)}<div class=panel><p>This workspace is connected to the shared reference-keyed fabric.</p>${x.tracking.slice(0,12).map(r=>`<div class=data><b>${esc(r.referenceId||'—')}</b><span>${esc(r.title)}</span><small>${esc(r.status)}</small></div>`).join('')||'<p>No records yet.</p>'}</div></div>`};
