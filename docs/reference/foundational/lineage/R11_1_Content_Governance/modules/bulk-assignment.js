import{State}from'../core/state.js';import{head,toast}from'../core/ui.js';export async function mount(el){const s=State.get();el.innerHTML=`<div class=workspace>${head('Bulk Assignment','Controlled bulk action with the existing cap unchanged.')}<form class="panel grid"><label class=wide>IDs<textarea name=ids required></textarea></label><label>Category<input name=category required></label><label>MAX_BULK_ASSIGN<input value="${s.settings.maxBulkAssign}" readonly></label><button class=btn>Apply</button></form></div>`;el.querySelector('form').onsubmit=e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target)),ids=d.ids.split(',').map(x=>x.trim());if(ids.length>s.settings.maxBulkAssign)return toast('Bulk limit exceeded');let n=0;s.activities.forEach(a=>{if(ids.includes(String(a.id))){a.category=d.category;n++}});State.patch({activities:s.activities});toast(n+' records updated')}}
