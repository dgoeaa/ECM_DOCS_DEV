import { State } from '../core/state.js';
import { normalize } from '../core/domain.js';
import { head, toast } from '../core/ui.js';
import { EndpointKeys } from '../config/endpoints.config.js';
export async function mount(el){const s=State.get();el.innerHTML=`<div class="workspace">${head('Settings','Profile, theme, density, limits, import and endpoint configuration.')}<form class="panel grid"><label>Name<input name="name" value="${s.profile.name}"></label><label>Email<input name="email" value="${s.profile.email}"></label><label>Persona<select name="persona"><option>admin</option><option>executive</option><option>registry</option><option>general</option></select></label><label>Theme<select name="theme"><option>government</option><option>dark</option><option>high-contrast</option></select></label><label>Density<select name="density"><option>comfortable</option><option>compact</option></select></label><label>MAX_BULK_ASSIGN<input name="max" type="number" min="1" value="${s.settings.maxBulkAssign}"></label>${EndpointKeys.map(k=>`<label class="wide">${k}<input name="${k}" value="${s.settings.endpoints[k]||''}" placeholder="https://"></label>`).join('')}<div class="wide"><button class="btn">Save</button> <button type="button" class="btn ghost" data-import>Import Activities</button><input hidden type="file" data-file accept="application/json"></div></form></div>`;const f=el.querySelector('form');f.persona.value=s.profile.persona;f.theme.value=s.settings.theme;f.density.value=s.settings.density;f.onsubmit=e=>{e.preventDefault();const d=Object.fromEntries(new FormData(f));const endpoints=Object.fromEntries(EndpointKeys.map(k=>[k,d[k]]));State.patch({profile:{name:d.name,email:d.email,persona:d.persona},settings:{theme:d.theme,density:d.density,maxBulkAssign:+d.max||50,endpoints}});document.documentElement.dataset.theme=d.theme;document.documentElement.dataset.density=d.density;toast('Settings saved','success');};const fi=el.querySelector('[data-file]');el.querySelector('[data-import]').onclick=()=>fi.click();fi.onchange=async()=>{const d=JSON.parse(await fi.files[0].text());const rows=Array.isArray(d)?d:(d.activities||d.value||[]);State.patch({activities:rows.map(normalize)});toast('Imported '+rows.length+' activities','success');};}
